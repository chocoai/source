<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jeesite.modules.asset.department.dao.DepartmentTok3Dao">
	
	<!-- 查询数据
	<select id="findList" resultType="DepartmentTok3">
		SELECT ${sqlMap.column.toSql()}
		FROM ${sqlMap.table.toSql()}
		<where>
			${sqlMap.where.toSql()}
		</where>
		ORDER BY ${sqlMap.order.toSql()}
	</select> -->
	<delete id="deleteData">
		delete from js_department_tok3 where department_code=#{arg0}
	</delete>
</mapper>