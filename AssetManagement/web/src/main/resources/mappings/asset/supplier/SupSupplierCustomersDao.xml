<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jeesite.modules.asset.supplier.dao.SupSupplierCustomersDao">
	
	<!-- 查询数据
	<select id="findList" resultType="SupSupplierCustomers">
		SELECT ${sqlMap.column.toSql()}
		FROM ${sqlMap.table.toSql()}
		<where>
			${sqlMap.where.toSql()}
		</where>
		ORDER BY ${sqlMap.order.toSql()}
	</select> -->
	<delete id="deleteData">
		delete from js_sup_supplier_customers where supplier_code=#{arg0}
	</delete>
</mapper>