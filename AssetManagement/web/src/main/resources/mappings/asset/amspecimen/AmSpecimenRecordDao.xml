<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jeesite.modules.asset.amspecimen.dao.AmSpecimenRecordDao">
	
	<!-- 查询数据
	<select id="findList" resultType="AmSpecimenRecord">
		SELECT ${sqlMap.column.toSql()}
		FROM ${sqlMap.table.toSql()}
		<where>
			${sqlMap.where.toSql()}
		</where>
		ORDER BY ${sqlMap.order.toSql()}
	</select> -->
	<delete id="deleteDb" >
	DELETE from js_am_specimen_product WHERE specimen_code=#{specimenCode}
	</delete>
</mapper>