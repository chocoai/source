<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jeesite.modules.asset.periodstate.dao.AmPeriodStateDao">
	
	<!-- 查询数据
	<select id="findList" resultType="AmPeriodState">
		SELECT ${sqlMap.column.toSql()}
		FROM ${sqlMap.table.toSql()}
		<where>
			${sqlMap.where.toSql()}
		</where>
		ORDER BY ${sqlMap.order.toSql()}
	</select> -->
	<!-- 删除数据-->
	<delete id="deleteDb">
		DELETE FROM js_am_period_state
		WHERE period_state_code=#{periodStateCode}
	</delete>
</mapper>